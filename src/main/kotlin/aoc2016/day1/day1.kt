package aoc2016.day1

import dev.johnvinh.getInput

fun move(currentX: Int, currentY: Int, newDirection: Char, previousDirection: Char): IntArray {
    return intArrayOf(currentX, currentY)
}

fun main() {
    val lines = getInput()
    val input = lines[0]
}